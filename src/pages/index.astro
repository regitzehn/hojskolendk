---
import MainLayout from "../layouts/MainLayout.astro";
import Button from "../components/Button.astro";
import Link from "../components/Link.astro";
import { Image } from "astro:assets";
import Hero from "../components/Hero.astro";
import Faq from "../components/Faq.astro";
import Banner from "../components/Banner.astro";
import Trin from "../components/Trin.astro";
import InfoModer from "../components/InfoModer.astro";
import RejserCard from "../components/RejserCard.astro";
import Instagram from "../components/Instagram.astro";
---

<MainLayout title="Titel">
	<!-- 🌍 Hero Section -->
	<Hero />

	<!-- ✨ Intro Section -->
	<section class="content-wrapper my-16 md:my-24 lg:my-32 fade-up-on-scroll">
		<h1 class="h1 pb-4">DIT LIVS EVENTYR</h1>
		<p class="p max-w-[55ch] text-[var(--color-sort)]">
			HÖJSKOLENDK er for dig, der vil på eventyr med højskolefællesskabet i
			rygsækken. Vi rejser ud i verden for at opleve, lære og møde nye mennesker
			og kulturer.
			<br /><br />
			Oplev fællesskab, mening og masser af rejseglæde på vores højskoleophold i
			udlandet. Tryghed er altid med på turen – vi er medlem af
			Rejsegarantifonden.
			<br /><br />
			Klar til dit næste eventyr?
		</p>
	</section>

	<!-- 🪜 Trin Section (Parallax) -->
	<section
		class="relative w-full bg-fixed bg-center bg-cover text-[var(--color-hvid)] py-24 text-center"
		style="background-image: url('public/trin.webp');"
	>
		<div class="absolute inset-0 bg-[rgba(0,0,0,0.25)]"></div>

		<div class="relative z-10 content-wrapper flex flex-col items-center fade-up-on-scroll">
			<h2 class="h2 mb-4">SÅDAN FUNGERER DET (TRIN FOR TRIN)</h2>
			<p class="p max-w-[70ch] mb-12">
				Få overblik over, hvordan du kan komme med på en rejse med HøjskolenDK.
				Vælg, om du vil reservere en plads, tilmelde dig, eller blot høre mere.
			</p>

			<div class="flex flex-col items-center gap-6 mb-10">
				<Trin
					title="1. Reserver en plads"
					text="Find din drømmerejse og reserver en plads – helt uforpligtende. Så holder vi en plads til dig, mens du beslutter dig."
					shade="var(--color-gron-1)"
				/>
				<Trin
					title="2. Tilmeld dig rejsen"
					text="Klar til eventyret? Udfyld tilmeldingsformularen og køb din billet. Så er du officielt med på holdet!"
					shade="var(--color-gron-2)"
				/>
				<Trin
					title="3. Deltag i et infomøde (valgfrit)"
					text="Vil du høre mere først? Tilmeld dig et af vores infomøder og få svar på alle dine spørgsmål."
					shade="var(--color-gron-3)"
				/>
			</div>

			<Button href="/ophold" text="VÆLG DIT EVENTYR" color="hvid" size="lg" />
		</div>
	</section>

	<!-- 🌏 Rejser Cards -->
	<div class="fade-up-on-scroll my-16">
		<RejserCard />
	</div>

	<!-- 💬 Infomøder Section (Parallax) -->
	<section
		class="relative w-full bg-fixed bg-center bg-cover text-[var(--color-hvid)] py-24 text-center"
		style="background-image: url('public/informøder.webp');"
	>
		<div class="absolute inset-0 bg-[rgba(0,0,0,0.25)]"></div>

		<div class="relative z-10 content-wrapper flex flex-col items-center fade-up-on-scroll">
			<h2 class="h2 mb-4">INFOMØDER</h2>
			<p class="p max-w-[70ch] mb-12">
				Kig forbi et af vores hyggelige infomøder i København, Aarhus eller online – vi glæder os til at møde dig!
			</p>

			<div class="flex flex-col items-center gap-6 mb-10">
				<InfoModer />
			</div>
		</div>
	</section>

	<!-- 🧡 Banner -->
	<div class="fade-up-on-scroll">
		<Banner />
	</div>
	
 <Instagram />
	<!-- 🌿 Vi Gør En Forskel Section (Parallax) -->
	<section
		class="relative w-full bg-fixed bg-center bg-cover text-[var(--color-hvid)] py-28 text-center"
		style="background-image: url('public/vigørenforskel.webp');"
	>
		<div class="absolute inset-0 bg-[rgba(0,0,0,0.35)]"></div>

		<div class="relative z-10 content-wrapper max-w-[850px] mx-auto fade-up-on-scroll">
			<h2 class="h2 mb-6 font-[Montserrat] tracking-wide">VI GØR EN FORSKEL</h2>

			<p class="p mb-4 text-lg leading-relaxed font-[Poppins]">
				Sammen med <em>‘Trees For the Future’</em> planter vi 50 træer for hver elev,
				der rejser med os på højskoleophold i udlandet.
			</p>

			<p class="p text-lg leading-relaxed font-[Poppins] mb-10">
				I et spændende samarbejde med <strong>Verdens Skove</strong>, har vi fredet
				<strong><em> 106.785 m² </em></strong> regnskov i Nicaragua – og vi fortsætter.
			</p>

			<img
				src="public/verdensskove.svg"
				alt="Verdens Skove Logo"
				class="mx-auto w-[220px] opacity-95"
			/>
		</div>
	</section>

	<!-- 💬 FAQ Section -->
	<div class="fade-up-on-scroll">
		<Faq />
	</div>

	<!-- ✨ Animations + Parallax Styles -->
	<style>
		@keyframes fade-up {
			0% {
				opacity: 0;
				transform: translateY(40px);
			}
			100% {
				opacity: 1;
				transform: translateY(0);
			}
		}

		.fade-up-on-scroll {
			opacity: 0;
			transform: translateY(40px);
			transition: opacity 0.8s ease-out, transform 0.8s ease-out;
		}

		.fade-up-on-scroll.visible {
			opacity: 1;
			transform: translateY(0);
		}

		section.bg-fixed {
			background-attachment: fixed;
			background-position: center;
			background-size: cover;
		}
	</style>

	<script>
		// Fade-up animation when element enters viewport
		const fadeEls = document.querySelectorAll(".fade-up-on-scroll");
		const observer = new IntersectionObserver(
			(entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						entry.target.classList.add("visible");
						observer.unobserve(entry.target);
					}
				});
			},
			{ threshold: 0.15 }
		);
		fadeEls.forEach((el) => observer.observe(el));
	</script>
</MainLayout>